/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ['./build/*.html'], // this is telling the computer "to look in the build file and take all the html files"
  theme: {
    extend: {},
  },
  plugins: [],
}
